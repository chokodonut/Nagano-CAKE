<h1>商品一覧</h1>
<% @items.each do |item| %>
<% if @items.image.attached? %>
  <%= image_tag @items.image, size: "200x200" %>
<% else %>
  <%= image_tag 'no_image', size: "200x200" %>
<% end %>
  <%= item.name %>
  <%= item.price %>
<% end %>